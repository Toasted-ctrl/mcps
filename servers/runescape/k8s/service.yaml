apiVersion: v1
kind: Service
metadata:
  name: mcp-runescape-service
  namespace: mcp-runescape
spec:
  selector:
    app: mcp-runescape
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP