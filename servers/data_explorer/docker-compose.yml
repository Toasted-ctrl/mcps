services:
  data-explorer:
    build:
      context: ../../
      dockerfile: servers/data_explorer/Dockerfile
    container_name: data-explorer-mcp
    env_file:
      - .env
    ports:
      - "10005:8000"
      - "10006:9000"
    restart: unless-stopped