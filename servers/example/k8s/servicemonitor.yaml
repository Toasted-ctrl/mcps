apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mcp-example
  namespace: mcp-example
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: mcp-example
  endpoints:
    - port: metrics
      interval: 15s
      path: /metrics