apiVersion: v1
kind: Service
metadata:
  name: mcp-example-service
  namespace: mcp-example
spec:
  selector:
    app: mcp-example
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP