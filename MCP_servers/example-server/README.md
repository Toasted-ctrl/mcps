# Contents
This directory contain an example set-up for rapid MCP development, including logging as well as metrics exposure through a dedicated Prometheus port. To get started, rename `.env.example` to `.env`, and after run
```bash
docker compose up
```